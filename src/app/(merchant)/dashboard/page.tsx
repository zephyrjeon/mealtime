import { RegisterStore } from './_components/RegisterStore';

const DashboardPage = () => {
  const hasRegistered = false;

  return (
    <div className="w-full h-full">
      <RegisterStore />
    </div>
  );
};

export default DashboardPage;
